# examples

Fixtures the compiler and the signal rule must match. No media files.

| Directory | Proves |
| --- | --- |
| [hook-v3-body-v1-9x16/](hook-v3-body-v1-9x16/) | `hook_rate` on the declared `[0, 90)` window dirties hook + hook→body kerf; body stays clean |
| [dub-en-9x16/](dub-en-9x16/) | Dirty-set contract on a 10s clock: picture → dub-en inherit; `vo_hold` misses body; explicit 3–5s `note`; `hold` names body without inventing speed. Not a dubbed film (`audio_mix` = spine AAC + overlay) |
| [ship-swap/](ship-swap/) | User walk on generated media (`make demo`, `make walkthrough`). No clips in git |
