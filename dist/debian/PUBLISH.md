# Debian ITP

Needs Gmail. Local postfix queued a copy as
`abinash@Abinashs-MacBook-Air.local`; Debian BTS will bounce that From.

1. Open Gmail as `abinashsena@gmail.com`.
2. Compose **plain text**. Paste [`ITP`](ITP) (headers + body).
3. To: `submit@bugs.debian.org`
4. Subject: `ITP: graft -- local-first video composition workspace`
5. Send. Reply in the graft repo with the BTS number (`#nnnnnn`).
